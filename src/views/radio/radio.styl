.item-lists {
	margin-bottom .4rem;
}
.item-title {
	$public-title();
}
.lists {
	padding 0 .2rem;
	.list {
		flex 0 0 50%;
		padding .2rem 0 .2rem .2rem;
	}
	.item-img {
		width 1rem;
		height 1rem;
		margin-right .18rem;
	}
	.song {
		color $cfff;
	}
}